name: Init GitHub Pages Branch

on:
  workflow_dispatch:

jobs:
  create-gh-pages:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout main branch
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Create gh-pages branch
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"

          # Клонируем репозиторий заново для чистой работы с ветками
          git clone https://x-access-token:${GH_PAT}@github.com/${{ github.repository }} repo
          cd repo

          # Если ветки нет — создаём
          if ! git ls-remote --exit-code --heads origin gh-pages; then
            git checkout --orphan gh-pages
            git rm -rf . || true
            echo "GitHub Pages branch initialized" > index.html
            git add index.html
            git commit -m "Initialize gh-pages branch"
            git push origin gh-pages
          else
            echo "gh-pages branch already exists, nothing to do"
          fi
